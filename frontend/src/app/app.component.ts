import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  // Pointing to the file where the HTML is located
  templateUrl: 'app.component.html'
})
export class AppComponent {
  
}
